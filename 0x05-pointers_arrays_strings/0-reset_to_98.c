#include <stdio.h>
#include "main.h"
/**
 * Writes a function that takes a pointer to an int as
 * parameter and updates the value it points to 98
 * @n: input variable for pointer n
 * Return: n(input variable)
 */
void reset_to_98(int *n);
{
	*n = 98;
}
